# Django
SECRET_KEY=replace-this-with-a-long-random-value
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Set DB_ENGINE=mysql and populate these values when moving off local SQLite.
DB_ENGINE=sqlite
DB_NAME=freethedesk_db
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

